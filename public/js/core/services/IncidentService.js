/**
 * 🚨 INCIDENT SERVICE
 * Service layer for incident management.
 */

const IncidentService = (store) => {
  const createIncident = (data) => {
    // Logic to create an incident
    console.log('IncidentService: Creating incident');
  };

  return {
    createIncident,
  };
};

export default IncidentService;
